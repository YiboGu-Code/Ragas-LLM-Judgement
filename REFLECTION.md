# REFLECTION：项目反思报告（1500–2500 字）

回顾我在本次 AI4SE 期末项目中，使用 Superpowers 方法论与编码智能体协作完成一个可部署的 LLM 测评平台的全过程，并对关键取舍、工作流收益与不足进行反思。

为便于验收与自证，本反思会尽量引用仓库中的具体文件与产物：例如 [SPEC.md](file:///e:/Homework/SEEC3/RagasTest/SPEC.md)、[PLAN.md](file:///e:/Homework/SEEC3/RagasTest/PLAN.md)、[SPEC_PROCESS.md](file:///e:/Homework/SEEC3/RagasTest/SPEC_PROCESS.md)、[README.md](file:///e:/Homework/SEEC3/RagasTest/README.md) 以及测试目录 `tests/` 与前端目录 `frontend/`。

## 1. 项目一句话总结

- 我做了什么：我实现了一个面向 Prompt / RAG / Workflow / Agent 四类场景的 LLM 评测平台，核心闭环是“上传 JSONL 数据集 → 创建 Run → 启动执行 → 查看逐条结果 → 导出 CSV/JSON/JSONL”。后端使用 FastAPI + SQLite + 本地 artifacts，前端使用 React + TypeScript + Vite，并提供 Docker Compose 一键部署。
- 解决了谁的什么问题：它面向需要对 LLM 应用做可复用评测与回归验证的人，解决了“评测数据与指标难复用、结果不可追溯、导出不统一、部署不方便”的问题，尤其强调 dataset-only 模式，不依赖外部 SUT。
- 最终交付形态：后端 API（FastAPI）、前端可视化界面（React）、一键部署（Dockerfile + docker-compose.yml），并提供线上部署地址（见 [README.md](file:///e:/Homework/SEEC3/RagasTest/README.md) 的“在线访问”）。

## 2. Superpowers 在本项目里的效果评估

### 2.1 哪些技能发挥最大作用？

1. `brainstorming`：它最大的价值是把“想做一个评测平台”这种模糊目标，逼迫我落到可执行的约束上，尤其是：

- 先锁定不可逆的边界：数据从哪里来（JSONL 数据集为核心）、是否需要 SUT（dataset-only 优先）、如何持久化（SQLite 默认）、执行方式（进程内异步并发）。
- 把“通用性”落在接口与协议上：后端通过 Provider/SUTAdapter/Metric 的抽象边界实现可插拔（这一点也反映在 [FRONTEND_HANDOFF.md](file:///e:/Homework/SEEC3/RagasTest/FRONTEND_HANDOFF.md) 对核心概念与接口的强调）。

2. `writing-plans`：计划文件 [PLAN.md](file:///e:/Homework/SEEC3/RagasTest/PLAN.md) 将任务拆成了可以逐步推进的单元，并在“进度”部分持续记录了完成的 commit hash。这让我在需求多次变化时，仍能以清单形式保持对项目状态的把控，避免“做到哪算哪”的漂移。

3. `test-driven-development`：TDD 的好处不只是覆盖率，而是把需求变成可执行断言，逼迫我在实现前就回答：

- 哪些输入应该被接受/拒绝？
- 哪些错误应该返回 404/409/422，响应结构长什么样？
- 在缺字段时是 `skipped` 还是 `failed`，细节如何解释？

这些都直接影响到平台的可用性与一致性。

4. `verification-before-completion`（以及我在后续工作中形成的“先证据后宣称完成”习惯）：它让我在每次功能补齐后都回到可执行命令与证据。例如后端侧在 [SPEC_PROCESS.md](file:///e:/Homework/SEEC3/RagasTest/SPEC_PROCESS.md) 里记录了 `pytest`、`ruff`（或等价静态检查）等验证证据；前端侧我也用 `npm run lint`、`npm run build`、`npm test` 来确保修改不会引入回归。

### 2.2 哪些步骤让我觉得“形式感较强”，但仍有价值？

我感受到“形式感”最强的部分是：把每个环节都严格映射为固定流程（brainstorming → plan → worktree → subagent → TDD → review），在需求快速变化时会显得繁琐。

但从结果看，这个流程仍然有价值：它会逼我在“继续加功能”之前先回答“这个功能是否真的属于本期范围、是否能被验证、是否会破坏已有契约”。换句话说，流程的“慢”换来的是“少返工、少走偏”。

对我来说最“麻烦但有用”的环节是强制验证：每做完一项都要重新跑测试、构建或接口检查，短期看会打断连续编码的节奏，但它确实逼我用证据而不是感觉判断“是否完成”，也因此减少了不少“本地看着没问题、实际一跑就坏”的返工。

## 3. TDD 在 AI 协作下是阻碍还是放大器？

我的体感是：在 AI 协作场景里，TDD 更像放大器，而不是阻碍。原因是 AI 很容易在需求不够明确时“编一个看似合理的实现”，而测试能把“看似合理”变成“必须可证明”。

### 例子 1：Dataset 删除接口（后端）用例驱动

我新增了 dataset 删除能力，并用测试把行为锁死（参考 `tests/test_datasets_delete_api.py`），覆盖了：

- dataset 不存在返回 404；
- dataset 被 run 引用时返回 409；
- 删除成功返回 204，并且 raw 文件会被清理（如果存在）。

当时出现过典型的“405 Method Not Allowed”，本质就是后端路由未实现或未生效，测试让问题能稳定复现，从而快速定位到“缺少 DELETE 路由”这一根因。

### 例子 2：导出下载行为（前端）通过可验证行为收敛

导出需求看似只是“导出 JSON/JSONL”，但在真实使用中更关键的是“必须以文件下载方式触发，并带正确后缀”。因此前端实现采用 Blob 下载，并通过工具函数统一文件名（对应 `frontend/src/utils/exportFilename.ts` 与其测试 `frontend/src/utils/exportFilename.test.ts`）。这类测试的价值在于把“应该是什么文件名”这种细节变成自动化约束，避免 UI 改动时悄悄回归。

### 成本与代价

TDD 的主要代价不是写断言本身，而是：

- 测试环境的稳定性与跨平台差异（例如 async 测试后端 anyio backend 的固定、Windows 环境命令差异等）。
- 初期写测试会更慢，但当需求多次迭代时，测试能显著降低“改了 A 影响了 B”的风险。

我觉得写测试最痛苦的点不是断言本身，而是测试环境与真实运行环境的贴合，尤其是涉及异步执行、Docker、前后端联调以及 LLM 调用时，很多时间其实花在让测试“稳定、可复现、不过度依赖外部环境”上。

## 4. subagent-driven 工作流：能自主跑多久不偏题？

我的经验是：subagent 更适合承担“边界清楚、输入输出可验证”的任务。

- 适合交给 subagent 的 task：接口实现、增量测试补齐、UI 小功能、Docker/Compose 配置调整、错误码对齐与导出格式规范等。
- 需要我人工主导的 task：范围裁剪与优先级（例如明确“dataset-only 为主，不做依赖 SUT 的工作”）、安全边界（例如密钥绝不进前端/镜像构建阶段）、以及在需求频繁变化时的整体一致性把控（比如“Help 页面说明”“demo 数据集下载”“删除行为的 409 语义”需要一致）。

我观察到比较合适的 task 粒度是：能在一个会话里讲清“要改哪些文件、要跑哪些验证命令、预期输出是什么”，并且能用现有测试或新增测试作为验收。

## 5. SPEC / PLAN 的质量如何影响实现质量？

我在这个项目里明显体会到：SPEC/PLAN 写得越清楚，AI 越不容易跑偏，自己也越不容易“做到一半才发现需求不对”。

### 5.1 两个“规约不清晰导致返工/跑偏”的点

1. 需求范围变化：最开始的文档与计划强调“仅后端”，但实际需求后来追加了前端与部署，导致我必须重新补齐：

- 前端工程（`frontend/`）与页面结构；
- Nginx 反代与 docker-compose 统一部署；
- UI 帮助文档与 demo 数据集下载等“可用性功能”。

这说明如果在最开始就把“是否需要 UI 与演示部署”写进 SPEC，后续返工会更少。

2. “删除/导出”这类看似简单的功能，细节其实决定体验：例如删除 Run 出现 405 的原因可能来自路由未挂载、预检、或代理路径差异；导出 JSON/JSONL 如果不是下载文件，对用户就是“看到了内容但拿不到文件”。这些需求细节如果不写进规约，很容易被 AI 做成“能跑但不好用”的版本。

### 5.2 一个“规约写得好带来收益”的点

dataset-only 的核心约束（record 必须提供 output 或 trace、缺字段指标必须 skipped、provider 密钥不进前端等）一旦写清楚，后续前后端联调就更顺：UI 选择 metrics 时能解释“为什么会 skipped”，而不是把 skipped 当成 bug。

对我帮助最大的一点其实是“明确不做什么”：例如优先支持 dataset-only、provider 密钥不进入前端、删除行为必须有明确 404/409 语义，这些边界一旦写清楚，后续很多实现分歧都能提前避免。

## 6. 我最有效的 prompt / context 策略

我总结出三条在这个项目里最有效的协作策略（它们本质上是在“把任务变成可验证的工程工作”）：

1. 先明确验收，再写实现：无论后端还是前端，我都会先把“成功标准”写成可执行检查（测试、lint、build、curl/healthz），再推进实现。

2. 把“边界条件”当成第一等公民：对每个 API 我都会问自己至少三个问题：404 怎么返回？409/422 怎么区分？错误信息是否能指导用户修复？

3. 优先用最少可行方案，避免过度设计：例如 demo 数据集下载选择用前端静态资源发布，而不是改后端增加额外 API；部署选择 compose + Nginx 反代 `/api`，减少 CORS 与环境差异。

## 7. 关键取舍与偏离（必须如实）

我需要如实承认几个偏离点，并说明原因与补救：

1. 第二智能体冷启动验证：这项工作实际上已经完成，问题在于当时没有及时把证据写回 [SPEC_PROCESS.md](file:///e:/Homework/SEEC3/RagasTest/SPEC_PROCESS.md)，导致文档一度呈现为“未执行/待补充”。现在我已按仓库中的任务记录、实现轨迹与过程文档回溯补齐该节，明确这次冷启动试跑聚焦于任务 2（插件接口与 registry）和任务 3（数据集 schema 校验），并把它暴露出的规约缺口与后续修订一并写回。

2. PR/worktree 流程：我在迭代中更多使用了直接提交与强制推送覆盖远端的方式（尤其是在对接 GitHub/GitLab 的场景），这与“每个 worktree 对应一个 PR”的理想流程存在差距。补救方式是：后续可以在 GitLab 上以 MR 的形式补齐关键节点说明，或者在仓库里补充更细的分支与 PR 记录。

3. 镜像发布到 Docker Hub/GHCR：由于网络与平台访问限制，我采用了镜像导出为 tar 的交付方式（见 [docker-images-build.md](file:///e:/Homework/SEEC3/RagasTest/docker-images-build.md)），而不是公开镜像仓库发布。若课程严格要求公开镜像，可在网络条件允许时补做 GHCR 发布与 CI 推送。

## 8. 如果重做一次，我会改变什么？

1. 更早锁定范围：在 brainstorming 阶段就明确“是否包含前端与部署”，避免后期大范围返工。
2. 更早补齐“用户体验型需求”的验收：例如导出必须下载、删除必须有确认与错误解释、Help 页面必须写清指标含义与 skipped 规则。
3. 对网络/平台依赖做预案：提前准备国内可访问的镜像源、以及 GitHub/GitLab 的推送与权限策略，避免临近交付时被“无法拉取镜像/受保护分支”等非功能问题卡住。

## 9. 对 agentic SE 的总体看法（批判性）

我认为 Superpowers 的方法论在课程项目里非常有效，因为它用流程把“工程纪律”固化下来，特别是：

- 通过 TDD 把“需求”落为“可验证行为”，避免 AI 生成的实现漂移；
- 通过计划与检查清单把复杂任务拆小，让智能体能在清晰边界内持续推进；
- 通过“验证先于完成”避免把“看起来能用”误判为“真的可交付”。

但它也隐含了一个前提：使用者必须愿意花时间维护规范与证据。否则流程会变成形式，而不是质量保障。我的收获是：在 AI 能快速产出代码的前提下，人类工程师的价值更集中在“定义问题、设定边界、明确验收、保持一致性与可维护性”这些事情上。

如果让我选择，我最倾向把这种工作流优先用于团队项目，其次是范围较大的个人项目。因为当多人协作、需求经常变化、代码需要长期维护时，SPEC、PLAN、TDD 和验证证据带来的收益会被放大，它们能显著降低沟通成本，也能减少“每个人理解的完成标准都不一样”的问题。相反，在很小的一次性脚本或试验里，这套流程的成本可能会显得偏高。我最担心它在真实团队落地时的阻力主要有两点：第一，大家是否愿意为“过程证据”持续投入时间；第二，业务压力一大时，团队很容易重新滑回“先做出来再说”的短期心态。也就是说，这套方法真正的难点不在工具，而在团队是否愿意把规范、验证和一致性当成长期收益，而不是短期负担。
